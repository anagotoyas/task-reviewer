import { Title, Text, Stack } from '@mantine/core';

export function HomeworksPage() {
  return (
    <Stack gap="md">
      <Title order={3}>Tareas</Title>
      <Text c="dimmed">Gestión de tareas — próximamente.</Text>
    </Stack>
  );
}
