import { Title, Text, Stack } from '@mantine/core';

export function StudentCoursesPage() {
  return (
    <Stack gap="md">
      <Title order={3}>Mis cursos</Title>
      <Text c="dimmed">Vista de cursos del estudiante — próximamente.</Text>
    </Stack>
  );
}
