import { Title, Text, Stack } from '@mantine/core';

export function CoursesPage() {
  return (
    <Stack gap="md">
      <Title order={3}>Cursos</Title>
      <Text c="dimmed">Gestión de cursos — próximamente.</Text>
    </Stack>
  );
}
